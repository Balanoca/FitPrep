server.port=${PORT:8080}
spring.application.name=fitprep

# PostgreSQL — Supabase
spring.datasource.url=${DB_URL:jdbc:postgresql://db.jucxdoqjxzoezxrvmrlf.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Bravo160697182560}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway — desactivado porque ya corrimos las migraciones manualmente
spring.flyway.enabled=false
